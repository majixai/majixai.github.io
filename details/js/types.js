/**
 * @typedef {object} IProduct
 * @property {number|string} id
 * @property {string} name
 * @property {number} price
 * @property {number} features - A bitmask of features.
 * @property {(feature: number) => boolean} hasFeature
 */

// This file is used for JSDoc type definitions (simulating interfaces).
// It doesn't need to export anything.
